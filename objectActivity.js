// Define the named car with the  initial properties 
let car = {
  type: "SUV's",
  model: "Corolla Cross",
  color: "Black"
};

// Display the type of car in the console
console.log(typeof car);

// Modify the type of the car to Toyota and use console to update
car.type = "Toyota";
console.log(car);

// Add the new property of car  with a value of 4 wheels and use console to update the object
car.wheels = 4;
console.log(car);// Define the object named car with the initial properties
let car = {
  type: "SUV's",
  model: "Corolla Cross",
  color: "Black"
};

// Display the type of car using the console
console.log(typeof car);

// Modify the type of the car  to Toyota and log the updated object
car.type = "Toyota";
console.log(car);

// Add the new property of car with a value of 4 wheels and use console to updated the object
car.wheels = 4;
console.log(car);